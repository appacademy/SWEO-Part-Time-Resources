import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
// To run the bonus files, comment out the preceding line and uncomment the
// following line.
// import App from './bonus/App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
