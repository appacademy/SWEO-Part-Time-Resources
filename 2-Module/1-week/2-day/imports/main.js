const { printJesse } = require('./jesse')
const { x } = require('../../../importThis')

// const greeting = sayGreeting()
const name = printJesse()

// console.log(greeting + name)
console.log(x)
  ``
