### TEST BINARY Solution from PA
- drag and drop `binary-to-string.js` (must export as `binaryToString`) 
  - into `test-your-binary` folder

- run: `node binary-test.js`
- copy content from .txt file and paste to google sheets~!