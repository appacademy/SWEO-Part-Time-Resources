class KeyValuePair {
  constructor(key, value) {
    this.key = key;
    this.value = value;
  }
}
// Hashing
//SimpleHash

//SHA256

class HashTable {
  constructor(size = 8) {
    this.data = new Array(size).fill(null);
  }
  // Hash Modulo

  // Inserting Key/Vals

  // Reading Key/Vals
}

let hs = new HashTable();
console.log(hs.data)