// (T/F) Browser Object Model (BOM) represents a hierachy of other browser objects. We can use Javascript to communicate with these objects and interact with our browser
// answer:

// (T/F) The Window Object is at the top of the hierarchy
// answer:

// (T/F) The Document Object Model (DOM) is part of the BOM hierachy tree through the window object:
// answer:

// (T/F) DOM represents the hiearchy that contains a collection of nodes that represent HTML elements aka the web page. It can be accessed using the window object `window.document`
// answer:

