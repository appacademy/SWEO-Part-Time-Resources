//Window.onload vs DOMContentLoaded

//Window.onload waits for literally EVERYTHING to load including the DOM, but also all assets (scripts, images);

//DOMContentLoaded only waits for the DOM to be loaded

//Event bubbling
